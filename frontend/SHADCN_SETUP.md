# shadcn/ui Setup

This project uses [shadcn/ui](https://ui.shadcn.com/) components with Tailwind CSS v4.

## Configuration

- **Tailwind Version:** v4 (using @tailwindcss/vite plugin, NOT traditional PostCSS)
- **shadcn Style:** new-york (minimal, clean variant)
- **Theme Integration:** shadcn CSS variables mapped to our Discord dark theme
- **Path Aliases:** `@/*` maps to `src/*`

## Installed Components

✅ button
✅ card
✅ input
✅ checkbox
✅ dialog
✅ scroll-area

## File Structure

```
frontend/
├── components.json          # shadcn/ui CLI configuration
├── src/
│   ├── components/
│   │   ├── ui/             # shadcn/ui components (auto-generated)
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── checkbox.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── input.tsx
│   │   │   └── scroll-area.tsx
│   │   └── ...             # Your app components
│   ├── lib/
│   │   └── utils.ts        # cn() utility function
│   └── index.css           # Includes shadcn CSS variables + Discord theme
```

## Adding New Components

```bash
npx shadcn@latest add <component-name>
```

Examples:
```bash
npx shadcn@latest add badge
npx shadcn@latest add select
npx shadcn@latest add textarea
```

Browse available components: https://ui.shadcn.com/docs/components

## Usage Example

```typescript
import { Button } from '@/components/ui/button'
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'

export function MyComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Example Card</CardTitle>
      </CardHeader>
      <CardContent>
        <Button>Click Me</Button>
      </CardContent>
    </Card>
  )
}
```

## Theme Colors

shadcn/ui components automatically use our Discord dark theme via CSS variables in `src/index.css`:

- Primary: Discord blurple (#5865f2)
- Background: Dark gray (#1e1f22)
- Cards: Slightly lighter gray (#2b2d31)
- Destructive: Red (#f23f43)

These are defined in both formats:
- `--accent` (Discord theme vars) → used by existing components
- `--primary` (shadcn vars) → used by shadcn/ui components

Both point to the same colors for visual consistency.

## Important Notes

- **Do NOT edit files in `src/components/ui/`** - they're auto-generated by shadcn CLI
- If you need to customize a component, copy it to a new file and modify
- The `cn()` utility in `src/lib/utils.ts` handles conditional class merging
- All components are fully typed with TypeScript
